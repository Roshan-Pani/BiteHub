# Database Layer (MongoDB)

## Start MongoDB

```bash
docker compose up -d
```

Run from this folder:

```bash
cd database
docker compose up -d
```

## Services

- MongoDB: mongodb://localhost:27017/bitehub
- Mongo Express UI: http://localhost:8081

## Stop

```bash
docker compose down
```
